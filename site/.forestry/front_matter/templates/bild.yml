---
label: Bild
hide_body: true
fields:
- type: field_group_list
  name: Gallery
  fields:
  - name: url
    type: file
    config:
      maxSize: 64
    label: URL
  - type: text
    name: de
    config:
      required: true
    label: Text (deutsch)
  - type: text
    name: en
    label: Text (englisch)
    config:
      required: true
  label: Bild
  config:
    labelField: de
pages:
- site/data/gallery.yaml
