---
label: Lebensabschnitt
hide_body: true
fields:
- type: field_group_list
  name: Biography
  fields:
  - type: text
    name: title-de
    label: Titel (deutsch)
    config:
      required: true
  - name: description-de
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: markdown
    label: Beschreibung (deutsch)
  - type: text
    name: title-en
    label: Titel (englisch)
    config:
      required: true
  - name: description-en
    type: textarea
    default: ''
    config:
      required: true
      wysiwyg: true
      schema:
        format: markdown
    label: Beschreibung (englisch)
  label: Lebensabschnitt
pages:
- site/data/biography.yaml
