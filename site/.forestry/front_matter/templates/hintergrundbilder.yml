---
label: Hintergrundbilder
hide_body: true
fields:
- name: blurLayout
  type: boolean
  label: Layout Blur
- name: default
  type: file
  config:
    maxSize: 64
  label: Standardbild
- name: '404'
  type: file
  config:
    maxSize: 64
  label: 404 Bild
- type: field_group
  name: sections
  fields:
  - name: home
    type: file
    config:
      maxSize: 64
    label: Start
  - name: events
    type: file
    config:
      maxSize: 64
    label: Events
  - name: press
    type: file
    config:
      maxSize: 64
    label: Presse
  - name: music
    type: file
    config:
      maxSize: 64
    label: Musik
  - name: biography
    type: file
    config:
      maxSize: 64
    label: Biographie
  - name: gallery
    type: file
    config:
      maxSize: 64
    label: Galerie
  - name: contact
    type: file
    config:
      maxSize: 64
    label: Kontakt
  label: Menüsparten
  description: Die Bilder sollten nicht größer als 2MB sein!
pages:
- site/data/background_images.yaml
