---
label: Start
hide_body: true
fields:
- name: image
  type: file
  config:
    maxSize: 64
  label: Profilbild
  description: optional
- type: textarea
  name: de
  config:
    wysiwyg: true
    schema:
      format: markdown
  label: Text (deutsch)
- type: textarea
  name: en
  config:
    wysiwyg: true
    schema:
      format: markdown
  label: Text (englisch)
pages:
- site/data/home.yaml
